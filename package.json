{
  "name": "mieruka-tax-monorepo",
  "private": true,
  "scripts": {
    "dev": "pnpm --recursive dev",
    "build": "pnpm --recursive build"
  },
  "engines": {
    "node": ">=24"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild"
    ]
  }
}
